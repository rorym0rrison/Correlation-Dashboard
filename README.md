# Correlation-Dashboard
Simple dashboard which allows the user to enter two tickers, download the data and then analyse the correlation between their returns.

To run the script insure that the following modules are installed in the environment:

-matplotlib
-datetime
-tkinter
-pandas
-numpy
-yfinance

Each module can be installed using the command "pip install PACKAGE NAME"
